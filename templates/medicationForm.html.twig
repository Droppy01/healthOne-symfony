{% extends "content_base.html.twig" %}

{% block main %}
<p class="h1"> todo </p>
    {{ form_start(form) }}
        {{ form_widget(form) }}

        <button class="btn btn-primary">create</buttonclass>
    {{ form_end(form) }}
{% endblock %}